language: node_js
node_js: stable
sudo: required
before_script:
- npm install -g polymer-cli
- polymer install
addons:
  firefox: latest
  apt:
    sources:
    - google-chrome
    packages:
    - google-chrome-stable

script: xvfb-run polymer test
dist: trusty
